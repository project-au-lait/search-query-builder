<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:if="ant:if" xmlns:unless="ant:unless" name="csv-loader" basedir=".">

  <taskdef resource="net/sf/antcontrib/antlib.xml" />

  <condition property="os.windows" value="true" else="false">
    <os family="windows" />
  </condition>

  <condition property="mvn.cmd" value="${basedir}\mvnw.cmd" else="${basedir}\mvnw">
    <os family="windows" />
  </condition>


  <target name="set-version">
    <property name="version" value="" />
    <fail
      if:blank="${version}"
      message="Please specify version in the system properties. (e.g. mvnw -Dant.target=set-version -Dversion=1.x.x"
    />

    <exec executable="${mvn.cmd}" dir="${basedir}">
      <arg line="versions:set -DnewVersion=${version} -DgenerateBackupPoms=false" />
    </exec>
  </target>


  <target name="deploy">
    <condition property="maven-central" value=",maven-central" else="">
      <not>
        <matches pattern="-SNAPSHOT$" string="${project.version}" />
      </not>
    </condition>

    <trycatch>
      <try>
        <exec executable="${mvn.cmd}" failonerror="true">
          <arg line="-f sqb-container -P setup-db" />
        </exec>

        <exec executable="${mvn.cmd}" failonerror="true">
          <arg line="-f sqb-container -P migrate-db" />
        </exec>

        <exec executable="${mvn.cmd}" failonerror="true">
          <arg line="install sonar:sonar -P analyze" />
        </exec>

        <exec executable="${mvn.cmd}" failonerror="true">
          <arg line="clean deploy -P release${maven-central}" />
        </exec>
      </try>

      <finally>
        <exec executable="${mvn.cmd}" failonerror="true">
          <arg line="-f sqb-container -P down-db" />
        </exec>
      </finally>
    </trycatch>
  </target>

</project>